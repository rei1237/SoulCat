export const metadata = { alternates: { canonical: "https://code-destiny.com/library/" } };
import FortuneLibrary from "@/components/FortuneLibrary";
export default function LibraryPage() {
  return <FortuneLibrary />;
}
